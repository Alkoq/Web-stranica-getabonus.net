# Environment variables za Hostinger deployment
# Zamenite sa vašim Neon PostgreSQL connection string-om

DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.neon.tech/database_name?sslmode=require

# Ostale environment varijable (opcionalno)
NODE_ENV=production
PORT=3000

# Session secret (zamenite sa random string-om)
SESSION_SECRET=your_random_session_secret_here_change_this

# Instrukcije:
# 1. Zamenite DATABASE_URL sa vašim Neon connection string-om
# 2. Promenite naziv fajla u .env (uklonite .hostinger)
# 3. Upload-ujte u root direktorijum gde je package.json